<?php

return [

    /*
    |--------------------------------------------------------------------------
    | โมดูลที่แสดงใน Application Access
    |--------------------------------------------------------------------------
    |
    | Recruit ยังเก็บ route และข้อมูลเดิมไว้ แต่ซ่อนจากหน้าเลือกระบบตามคำสั่ง
    | Manager หากกลับมาพัฒนาต่อให้ตั้ง INSIGHT_RECRUIT_VISIBLE=true
    |
    */

    'modules' => [
        'recruit_visible' => env('INSIGHT_RECRUIT_VISIBLE', false),
    ],

    /*
    |--------------------------------------------------------------------------
    | แจ้งเตือน PR Compare เมื่อข้อมูลพนักงานเปลี่ยน
    |--------------------------------------------------------------------------
    |
    | PR Compare (ระบบเปรียบเทียบราคาผู้ขาย) มิเรอร์ตาราง employees / app_users
    | ไปใช้ล็อกอินและแสดงผล เดิมต้องรอซิงค์ตามรอบ ทำให้ข้อมูลที่แก้ที่นี่ไปโผล่ช้า
    |
    | ตั้ง 2 ค่านี้แล้ว Insight จะยิงบอกทันทีที่มีการบันทึก (ดู App\Observers\PrCompareObserver)
    | เว้นว่าง = ปิดใช้งาน ไม่กระทบการทำงานของ Insight
    |
    | ⚠️ `pr_compare_secret` ต้องตรงกับ `INSIGHT_WEBHOOK_SECRET` ฝั่ง PR Compare
    |
    */

    'pr_compare_url' => env('PR_COMPARE_URL', ''),

    /*
    | ปลายทางเพิ่มเติม คั่นด้วยจุลภาค — ใช้ส่งให้เครื่อง dev ด้วย
    |
    | เวลาเทสต์ในเครื่อง dev จะได้ไม่ต้องไปแก้ข้อมูลใน Insight ของเครื่องตัวเองซ้ำอีกรอบ
    | แก้ที่ Insight บนเซิร์ฟเวอร์ที่เดียว แล้วไหลไปทั้งเซิร์ฟเวอร์และเครื่อง dev
    |
    | ⚠️ ทุกปลายทางต้องใช้ secret ตัวเดียวกัน
    | ⚠️ เครื่องที่ปิดอยู่ไม่ทำให้ Insight ช้า เพราะต่อไม่ติดใน 1 วินาทีแล้วข้ามไป
    */
    'pr_compare_urls' => env('PR_COMPARE_URLS', ''),

    'pr_compare_secret' => env('PR_COMPARE_SECRET', ''),

    /*
    |--------------------------------------------------------------------------
    | รูปพนักงานจากโฟลเดอร์ของ HR
    |--------------------------------------------------------------------------
    |
    | HR ถ่ายรูปพนักงานใหม่แล้ววางไว้ในโฟลเดอร์ share ตั้งชื่อไฟล์เป็นรหัสพนักงาน
    | เช่น 71100.jpg แยกโฟลเดอร์ตามปี พ.ศ. ที่ถ่าย
    |
    | คำสั่ง photos:pull อ่านจากที่นี่ ก็อปเข้า storage/app/public/profiles
    | แล้วผูกเข้ากับ employees.photo_path ให้เลย
    |
    | เป็น network share ที่ช้ามาก (ไฟล์รวมสองหมื่นกว่าไฟล์)
    | ปกติจึงสแกนเฉพาะโฟลเดอร์ปีปัจจุบัน ใช้ --all เฉพาะตอนตั้งระบบครั้งแรก
    |
    | เครื่องที่รันต้องเข้า share นี้ได้ ถ้าเข้าไม่ได้คำสั่งจะบันทึกไว้ในรายงาน
    | แล้วจบแบบไม่ทำอะไร ข้อมูลเดิมไม่เสียหาย
    |
    */

    'hr_photos' => [
        'path' => env('HR_PHOTOS_PATH', '//192.168.5.1/_DriveZ/_HR/4.Photos Employees'),

        // ชื่อโฟลเดอร์ปีใช้ปี พ.ศ. ต่อท้าย
        'year_folder_prefix' => env('HR_PHOTOS_YEAR_PREFIX', 'รูปพนักงานปี'),

        // ย่อรูปด้านยาวสุดเหลือกี่พิกเซล (ต้องเปิด extension gd ถึงจะย่อได้ ไม่งั้นก็อปไฟล์เดิม)
        'max_size' => (int) env('HR_PHOTOS_MAX_SIZE', 400),
        'jpeg_quality' => (int) env('HR_PHOTOS_JPEG_QUALITY', 82),

        // ให้ Laravel scheduler ยิง photos:pull เองไหม
        // เซิร์ฟ 192.168.7.12 ตั้งเป็น false เพราะ scheduler ที่นั่นรันด้วยบัญชี SYSTEM
        // ซึ่งเข้า network share ไม่ได้ — ใช้ Windows Task "InsightHrPhotosPull" ดึงแทน
        // (ดู HR_PHOTOS_SETUP.md)
        'schedule' => filter_var(env('HR_PHOTOS_SCHEDULE', true), FILTER_VALIDATE_BOOLEAN),
    ],

];
